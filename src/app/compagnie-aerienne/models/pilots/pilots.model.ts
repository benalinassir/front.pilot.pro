export class Pilot {
    numPilote: number;
    nomPilote: string;
    adresse: string;

    constructor(numPilote: number, nomPilote: string, adresse: string) {
        this.numPilote = numPilote;
        this.nomPilote = nomPilote;
        this.adresse = adresse;
    }
}
